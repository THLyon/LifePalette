<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LifePalette.Views.LoginPage">
	<StackLayout Padding="30" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Spacing="20">
		<Entry Placeholder="Email" x:Name="EmailEntry" HorizontalOptions="FillAndExpand" WidthRequest="300" />
		<Entry Placeholder="Password" IsPassword="True" x:Name="PasswordEntry" HorizontalOptions="FillAndExpand" WidthRequest="300" />
		<Button Text="Login" Clicked="OnLoginButtonClicked" HorizontalOptions="FillAndExpand" WidthRequest="300" />
		<Label HorizontalOptions="Center">
			<Label.FormattedText>
				<FormattedString>
					<Span Text="Don't have an account? Sign up " />
					<Span Text="here" TextColor="Blue" TextDecorations="Underline">
						<Span.GestureRecognizers>
							<TapGestureRecognizer Tapped="OnSignUpTapped" />
						</Span.GestureRecognizers>
					</Span>
				</FormattedString>
			</Label.FormattedText>
		</Label>
	</StackLayout>
</ContentPage>
